<%= locals.errorMessage %>
<div>
    <% if(typeof books !== 'undefined' && books.length > 0) { %>
        <% books.forEach(book => {  %>
            <img height="150" width="100" src="<%= book.coverImagePath %>" alt="">
        <% }) %>
    <% } %>
</div>